<br><div>
    <br><br><h1 id="h1" class="card z-depth-3 center-align">Articles</h1>
    <div class="row">
        <div class="col s3"></div>
    </div>
</div>
<div class="row">
    <div class="col s2"></div>
    <div class="col s8 left-align">
        {{#each article}}
        <div id="read" class="card-panel z-depth-3">
            <h4>{{this.title}}</h4>
            <a id="btnSignUp" href="/readArticle/{{this._id}}">Read</a>
        </div>
        {{/each}}
    </div>
</div>